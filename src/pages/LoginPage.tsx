import React from 'react';
import LogInCard from '../components/cards/LogInCard.tsx';

const LoginPage = () => {
  return (
    <div>
      {/* login page card */}
      <LogInCard />
      {/* recent log outs */}
    </div>
  );
};

export default LoginPage;
